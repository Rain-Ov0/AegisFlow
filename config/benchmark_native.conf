# 该文件只控制客户端负载，不读取、不改写服务端配置。
host = 127.0.0.1
port = 8080
request_concurrency = 2
connection_pool_size = 16
requests_per_connection = 1000
target_qps = 1000
# 每条连接处理到上限后重连，用于稳定制造连接 churn。
warmup_ms = 1000
duration_ms = 10000
connect_timeout_ms = 3000
request_timeout_ms = 3000
seed = 20260815
attack_ratio = 0.20
normal_users = 100000
normal_ip_count = 1000
normal_device_count = 5000
attack_users = 100
attack_ip = 203.0.113.10
attack_device = attack_device
entity_prefix = local
